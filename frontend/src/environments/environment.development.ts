/**
 * Configuration de DEVELOPPEMENT
 * ------------------------------------------------------------------
 * Utilisee automatiquement par `npm start`.
 * L'API PHP doit tourner en parallele sur le port 8000
 * (voir docs/setup.md).
 */
export const environment = {
  production: false,

  apiUrl: 'http://localhost:8000/api',
};
